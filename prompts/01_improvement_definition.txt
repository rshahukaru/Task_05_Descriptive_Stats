Define an “impact score” = z(pts_pg) + 0.5*z(shot_pct) − 0.5*z(to_pg if available else 0).
Most Improved Player (MIP) = the player with the highest impact score among GP ≥ 8.
Show the standardized components and the final score for the top-3.